import Router from "./Router";

/**
 * An instance of the Router class, used to manage the application's routes.
 *
 * @type {Router}
 * @see {@link Router}
 */
const router = new Router();

export default router;
